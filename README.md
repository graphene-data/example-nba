NBA Graphene example backed by BigQuery.

The project expects a `.env` file with `GOOGLE_CREDENTIALS_CONTENT` containing a service account JSON string that can read `intense-acumen-470417-u5.nba`.

Useful commands:

```bash
npm run serve
npm run graphene -- check
npm run graphene -- schema nba
```
